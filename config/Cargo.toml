[package]
name = "config"
description = "Printer/filament/process profile system with inheritance (stub until M2)."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
