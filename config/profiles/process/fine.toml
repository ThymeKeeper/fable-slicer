# 0.12mm fine quality: thicker shells (more layers), the dial toward finish.
# First layer stays at 0.2mm for adhesion (inherited from standard).
inherits = "standard"
layer_height_mm = 0.12
top_layers = 6
bottom_layers = 6
speed_quality = -0.6
