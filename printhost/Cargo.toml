[package]
name = "printhost"
description = "Moonraker (Klipper) HTTP client: upload g-code, start/pause/resume/cancel prints, query status."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
ureq = { version = "2", features = ["json"] }
serde_json = "1"
